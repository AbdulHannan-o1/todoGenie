apiVersion: v1
kind: Service
metadata:
  name: {{ include "todogenie.backend.fullname" . }}
  labels:
    {{- include "todogenie.labels" . | nindent 4 }}
    app: backend
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todogenie.selectorLabels" . | nindent 4 }}
    app: backend